# AvatarEditorUtils

Promise-based wrappers for Roblox `AvatarEditorService` APIs.

This package preserves the current upstream `AvatarEditorUtils` public API while using this fork's flat package requires and runtime validation conventions.
