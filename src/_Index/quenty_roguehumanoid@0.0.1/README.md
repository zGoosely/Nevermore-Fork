# RogueHumanoid

Imported from Nevermore's `@quenty/rogue-humanoid` package. It exposes
composable Rogue properties for humanoid WalkSpeed, jump settings, scale, and
maximum health through `RogueHumanoidService` and `RogueHumanoidServiceClient`.
