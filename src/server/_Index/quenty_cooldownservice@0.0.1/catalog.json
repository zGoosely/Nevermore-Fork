{
  "purpose": "`CooldownService` is a small, category-free cooldown package. A cooldown belongs to any `Instance` and a case-sensitive string key, so the same API can cover combat moves, tools, interactions, UI, matchmaking, or any other temporary lockout.",
  "description": "`CooldownService` is a small, category-free cooldown package. A cooldown belongs to any `Instance` and a case-sensitive string key, so the same API can cover combat moves, tools, interactions, UI, matchmaking, or any other temporary lockout.",
  "tags": [
    "data",
    "enums",
    "game_service",
    "gui",
    "input",
    "lifecycle",
    "localization",
    "math",
    "player_service",
    "reactive",
    "security",
    "serialization",
    "utility"
  ],
  "modules": {
    "CooldownService.luau": {
      "realm": "server",
      "kind": "Service",
      "description": "Creates, queries, observes, and expires authoritative cooldowns attached to arbitrary Instance owners. Public entry points include `CooldownService.Init`, `CooldownService.StartCooldown`, `CooldownService.StartTypedCooldown`, `CooldownService.RestartCooldown`, `CooldownService.GetCooldown`, `CooldownService.IsOnCooldown`, `CooldownService.GetRemainingTime`, `CooldownService.ObserveCooldown`."
    },
    "Server/Cooldown.luau": {
      "realm": "server",
      "kind": "Class/model",
      "description": "Authoritative server model for one tagged cooldown Folder. Public entry points include `Cooldown.new`, `Cooldown.Restart`, `Cooldown.AddTime`, `Cooldown.RemoveTime`, `Cooldown.Clear`."
    },
    "Server/CooldownBindersServer.luau": {
      "realm": "server",
      "kind": "Class/model",
      "description": "Provides the authoritative server Cooldown binder."
    },
    "Server/CooldownBurstTracker.luau": {
      "realm": "server",
      "kind": "Class/model",
      "description": "Tracks successful typed cooldown starts in rolling owner/key windows. Public entry points include `CooldownBurstTracker.new`, `CooldownBurstTracker.RecordUse`, `CooldownBurstTracker.Reset`, `CooldownBurstTracker.Destroy`."
    }
  }
}
