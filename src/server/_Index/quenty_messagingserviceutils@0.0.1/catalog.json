{
  "purpose": "Collects the messagingserviceutils runtime building blocks—Messaging Service Utils—and composes them for reuse in Roblox experiences.",
  "description": "Collects the messagingserviceutils runtime building blocks—Messaging Service Utils—and composes them for reuse in Roblox experiences.",
  "tags": [
    "game_service",
    "utility"
  ],
  "modules": {
    "MessagingServiceUtils.luau": {
      "realm": "server",
      "kind": "Class/model",
      "description": "Messaging Service Utils provides the class/model implementation used by the Messagingserviceutils package. Public entry points include `MessagingServiceUtils.promisePublish`, `MessagingServiceUtils.promiseSubscribe`."
    }
  }
}
